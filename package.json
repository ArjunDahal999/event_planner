{
  "name": "event_planner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate:latest": "pnpm --filter server run migrate:latest",
    "migrate:rollback": "pnpm --filter server run migrate:rollback",
    "seed:run": "pnpm --filter server run seed:run",
    "seed:make": "pnpm --filter server run seed:make",
    "db:reset": "pnpm --filter server run db:reset",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.12.4",
  "dependencies": {
    "knex": "^3.1.0",
    "mysql": "^2.18.1",
    "zod": "^4.3.6"
  }
}
